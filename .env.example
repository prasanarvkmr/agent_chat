# Kong AI Gateway Configuration
# Base URL of your Kong AI Gateway (without the route path)
KONG_AI_GATEWAY_BASE_URL=https://your-kong-instance.kongcloud.dev
KONG_API_KEY=your_kong_api_key_here

# LLM Model Selection
# Choose one of the following models:
#   OpenAI:    gpt-4, gpt-5, gpt-4-turbo
#   Anthropic: claude-sonnet, claude-opus
#   Google:    gemini-flash, gemini-pro
# 
# The app automatically routes to the correct Kong path:
#   gpt-* models     -> /openai route
#   claude-* models  -> /claude route
#   gemini-* models  -> /gemini route
LLM_MODEL=gpt-4

# Azure Databricks Configuration
DATABRICKS_SERVER_HOSTNAME=your-workspace.azuredatabricks.net
DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/your_warehouse_id
DATABRICKS_ACCESS_TOKEN=your_databricks_access_token

# Optional: Specify default catalog and schema
DATABRICKS_CATALOG=your_catalog
DATABRICKS_SCHEMA=your_schema
